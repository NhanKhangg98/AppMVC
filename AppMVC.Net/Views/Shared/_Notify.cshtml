@{
    var mgs = TempData["Notify"] as string;
}

@if (!String.IsNullOrEmpty(mgs))
{
    var notifyClass = mgs.StartsWith("Error") ? "danger" : "success";
    <div class="alert alert-@notifyClass alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        @mgs
    </div>
}
